import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carkey.ble',
  appName: 'CarKeyBLE',
  webDir: 'dist/public',
  bundledWebRuntime: false
};

export default config;
